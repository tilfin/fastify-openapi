{
  "name": "fastify-openapi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "schema": "typescript-json-schema ./src/schemas/**/*.ts \"*\" --out schema.json --topRef --required --id"
  },
  "devDependencies": {
    "@types/node": "^15.12.4",
    "typescript": "^4.3.4",
    "typescript-json-schema": "^0.50.1"
  },
  "dependencies": {
    "fastify": "^3.18.0",
    "fastify-swagger": "^4.8.0"
  }
}
